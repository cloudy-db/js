const IPFS = require('ipfs');
const OrbitDB = require('orbit-db');

class ezDb extends OrbitDB {
	/**
	 * @param {Object} ipfsOptions
	 * @returns {ezDb}
	 */
	constructor(ipfsOptions) {
		this.ipfs = 
	}
}